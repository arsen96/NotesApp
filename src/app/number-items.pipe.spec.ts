import { NumberItemsPipe } from './number-items.pipe';

describe('NumberItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
